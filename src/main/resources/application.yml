server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/blackSooooo/spring-config-server-sample.git
          search-paths: /config/
          default-label: master
          clone-on-start: true